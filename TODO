Release 1.0:

* display: erase old "=" and result

BUGS:


TODO:

* I can't draw a filled triangle. Axes of plane.
* Display numbers with fewer digits.

DONE:

* Calculation mode: put result on the plane
* plane: touch for selecting a number
* plane: pinch or spread for zooming
* plane: drag for moving
* When going into landscape, plane loses displayed numbers.
* buttons clear and reset in options menu

Release 2.0:

* z -> f(z) mode
* z -> |f(z)| mode
* x -> f(z) mode
* vibrate for bad results
* shaking for clear
* use fragments