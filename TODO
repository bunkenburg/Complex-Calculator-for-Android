Release 1.00:

* Font size on display is very small
* plane: swipe for scroll
* plane: reset
* plane: clear
* display: erase old "=" and result
* Input method service with a good keyboard for mathematics

Release 2.0:

* z -> f(z) mode
* z -> |f(z)| mode
* x -> f(z) mode

BUGS:

* When going into landscape, plane loses 1+i.

TODO:

* I can't draw a filled triangle. Axes of plane.

DONE:

* Calculation mode: put result on the plane
* plane: touch for selecting a number
